[package]
name = "minos"
version = "0.1.0"
edition = "2018"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
spin = { version = "0.9", default-features = false, features = ["spin_mutex", "lazy"] }